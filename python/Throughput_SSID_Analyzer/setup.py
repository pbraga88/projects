from distutils.core import setup
import py2exe
setup(
    console = [
        {
            "script": "Throughput_SSID_Analyzer.py" # Replace with the name of the script you want to convert. 
        }
    ],
    )